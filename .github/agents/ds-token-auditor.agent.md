---
description: "Alias — use `design-system-token-auditor.agent.md` as the single source of truth. Audit design tokens for hardcoded colors and missing CSS variables. Use when: check tokens, hardcoded hex, hardcoded rgb, hardcoded hsl, color audit, CSS variable compliance."
tools: [read]
---
# ds-token-auditor (Alias)

This file is a compatibility alias. Use `.github/agents/design-system-token-auditor.agent.md` for the authoritative specification, workflow, checks, and output format.

Do not update behavior in this file — edit the canonical agent instead.
